#include <stdio.h>

void main(){
	int i=0;
	char mchar[20];
	double j = 0;
	scanf("%d", &i);
	scanf("%lf", &j);
	scanf("%s", &mchar);
	printf("i is %d \n", i);
	printf("j is %ld \n", j);
	printf("mchar is %s \n", mchar);

}
